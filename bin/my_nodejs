#!/usr/bin/env sh

#installs to .local/var/npm

npm -g install express
npm -g install express-session
npm -g install express-generator
npm -g install formidable
npm -g install nodemailer
npm -g install mysql
npm -g install js-beautify
npm -g install komodo-debug
#npm -g install auto-changelog
#npm -g install @okta/oidc-middleware
#npm -g install @okta/okta-sdk-nodej
