==============================================================================
MY VIM RC                                                            *myvimrc*

Generated:
`stpl ~/dotfiles/etc/vim/doc/myvimrc.txt.stpl ~/dotfiles/etc/vim/doc/myvimrc.txt`

<%
from os.path import dirname as d, join as j
lns=[]
for filename in """vimrc
plugin/abbreviations.vim
plugin/autocommands.vim
plugin/digraphs.vim
plugin/functions.vim
plugin/python3vim.vim
plugin/settings.vim
plugin/statusbar.vim
plugin/wildignore.vim
plugin/mappings.vim
""".splitlines():
    with open(j(d(d(__file__)),filename)) as f:
        lns += f.readlines()
    end
end
%>
%def collect(lnstartswith):
%    for aln in sorted(ln[3:].strip() for
%       ln in lns if ln.startswith(lnstartswith)):
{{aln}}
%    end
%end

==============================================================================
MAPPINGS                                                           *myvimmaps*

%collect('"""')

==============================================================================
COMMANDS                                                       *myvimcommands*

%collect('"":')

==============================================================================
PLUGINS                                                            *myplugins*

%collect('""|')

 vim:tw=78:sw=4:ts=4:et:ft=help:norl:
