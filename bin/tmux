#!/bin/sh
# Wrapper for the tmux(1) program, so as to use the configuration file in
# XDG_CONFIG_HOME when creating a new session. Stolen from Sparrowhawk's dotfiles.

[ -f "$XDG_CONFIG_HOME"/tmux/tmux.conf ] ||
    { echo  >&2 "tmux configuration not found in XDG_CONFIG_HOME"; exit 1; }

/usr/bin/tmux -2 -f "$XDG_CONFIG_HOME"/tmux/tmux.conf "$@"
